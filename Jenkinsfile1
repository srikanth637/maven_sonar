pipeline {
        agent none
        stages {
         
          stage('build & sonarqube') {
            agent any
            steps {
              withSonarQubeEnv('Sonar-token') {
                echo 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }
